{
  "schema": "http://example.org/specification/v2.0.0",
  "metadata": {
  },
  "global": {
  },
  "components": {
    "IMAGE_METADATA": {
      "image_metadata.commandPath": "/usr/bin/docker",
      "image_metadata.options": "-d",
      "image_metadata.additionalParam": "--privileged -e SERVER=${IMAGE_METADATA_HOST} --net=host",
      "image_metadata.statusCommand": "docker inspect -f {{.State.Running}} ${CONTAINER_ID} | grep true"
    }
  }
}