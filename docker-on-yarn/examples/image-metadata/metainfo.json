{
  "schemaVersion": "2.1",
  "application": {
    "name": "IMAGE_METADATA",
    "components": [
      {
        "name": "IMAGE_METADATA",
        "type": "docker",
        "dockerContainers": [
          {
            "name": "metadata",
            "commandPath": "/usr/bin/docker",
            "image": "dstreev/image-metadata",
            "additionalParam": "--privileged -e SERVER=${IMAGE_METADATA_HOST} --net=host"
          }
        ]
      }
    ]
  }
}